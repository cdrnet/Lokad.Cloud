<?xml version="1.0" encoding="utf-8" ?>
<configuration>
	<configSections>
		<section name="autofac" type="Autofac.Configuration.SectionHandler, Lokad.Stack" requirePermission="false"/>
	</configSections>
	<autofac>
		<modules>
			<!-- This is the normal configuration that works agains the cloud
			<module type="Lokad.Cloud.Azure.StorageModule, Lokad.Cloud.Framework">
				<properties>
					<property name="DataConnectionString" value="DefaultEndpointsProtocol=https;AccountName=YOUR ACCOUNT NAME;AccountKey=YOUR ACCOUNT KEY" />
				</properties>
			</module>
			-->
			
			<!-- We use a configuration based on memory providers -->
			<module type="Lokad.Cloud.Mock.MockStorageModule, Lokad.Cloud.Framework">
				<!-- No properties needed -->
			</module>
		</modules>
	</autofac>
</configuration>